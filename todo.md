# USFM editor typing performance

Fixes to reduce lag when editing large books (e.g. Psalms), especially with the preview pane open.

## Todo

- [x] **Stop full parsing in `UsfmPane` on every keystroke** — `firstBookFromUsfm` calls `parse(usfm)` whenever `value` changes (~16 ms on Psalms). Replace with a lightweight `\c` marker scan, cached/incremental marker list, or debounced refresh separate from editor `onChange`.
- [x] **Stop lifting the full document string to React on every keystroke** — keep document state in CodeMirror; debounce or ref-sync to the workspace model for save/dirty tracking only.
- [x] **Unify validation** — share one debounced full-document parse between the CodeMirror linter and the shell errors panel instead of two independent parses (~250–300 ms apart).
- [x] **Pause scroll sync while typing** — defer split-pane scroll sync until typing idle, or sync only on explicit scroll; avoid `querySelectorAll` over the full preview DOM every ~120 ms while the cursor moves.
- [x] **Incremental syntax highlighting** — avoid full-document lexer passes and decoration rebuilds on every pause; scope to viewport or use incremental tokenization.

## Context

Preview HTML regeneration is debounced (700 ms) in split mode, driven from the live editor buffer (not the React value lift). Scroll sync is paused during typing and resumes after 500 ms idle. Workspace model updates are debounced (500 ms) while dirty state is marked immediately; save — and editor unmount — flush the buffer first. Syntax highlighting classifies only the visible viewport (plus margin) with a single in-flight latest-wins request (no fixed debounce). Validation is pushed by the language engine after each analysis and feeds both the errors panel and editor squiggles.

## Key files

- `packages/usfm-controls/src/components/usfm-pane/UsfmPane.tsx`
- `packages/usfm-controls/src/components/usfm-editor/UsfmEditor.tsx`
- `packages/usfm-controls/src/components/usfm-editor/codemirror-usfm.ts`
- `packages/usfm-controls/src/components/usfm-shell/UsfmShell.tsx`
- `packages/usfm-controls/src/components/usfm-pane/scroll-sync.ts`

# Rewrite parser in GO

I want to rewrite the USFM parser using GO lang. I want to keep the old parser for reference, but not include it in the final build. I want the new parser to be runnable as a standalone CLI tool and also includable as a GO library. I want the bible-edit app to include that library use it to find errors, do syntax highlighting, and provide intellisense (basically things the old parser is used for). I want the new parser to be modeled after the way the LSP protocol works except much simpler and tailored to the needs of USFM editing (e.g. we don't need a go-to-definition feature). Like the LSP protocol, the new parser engine should keep a copy of the text and stay in sync with the editor. Like the LSP, the new parser engine should run asynchronously. Unlike the LSP protocol, the new parser engine be integrated with the editor via the js/go integration mechanism in Wails and via regular function calls (not JSON-RPC).

I want the new parser engine to have good unit tests and good documentation in a README.md.

Ask clarifying questions as needed.

## Todo

### Scaffolding

- [x] **Create the Go module** — new Go module at `usfm-parser-go/` (top-level; `packages/` is for JS packages) separate from the `bible-edit` app so it is usable as a standalone library. Layout: `pkg/` (or root packages) for the library, `cmd/usfm/` for the CLI. Wire `go vet` / `go test` into `build.sh`.
- [x] **Keep the old TS parser for reference only** — leave `packages/usfm-parser/` in the repo but remove it from the workspace build/check/test pipeline (and eventually from `usfm-controls`/`usfm-model` imports) so it is not part of the final build. _Done: excluded from `build.sh` and root `deno.json` check/test tasks; README banner added. It stays a workspace member so remaining imports resolve; dropping it from the final app bundle happens when the "Switch features over" / preview-rendering items remove the last imports._

### Core parser (Go library)

- [x] **Port the grammar** — translate `packages/usfm-parser/src/grammar.ts` (marker definitions, nesting rules, attributes) into Go data structures. _Done: `usfm-parser-go/grammar/` with all grammar.test.ts cases ported._
- [x] **Port the lexer** — tokenizer equivalent to `lexer.ts`, producing tokens with byte/UTF-16 position info suitable for editor integration. _Done: `usfm-parser-go/lexer/`; all lexer.test.ts cases ported plus UTF-16 tests; token streams verified byte-identical to the TS lexer across all 66 BSB books (differential tool kept at `internal/lexdump`)._
- [x] **Port the parser and AST types** — equivalent of `parser.ts` / `types.ts`; error-tolerant parsing (never panic on malformed input, collect diagnostics instead). _Done: `usfm-parser-go/parser/` + flat `Node` AST in the root package; all parser.test.ts cases ported; ASTs and error lists verified identical to the TS parser across all 66 BSB books (`internal/astdump`)._
- [x] **Diagnostics** — structured errors/warnings with ranges and codes, matching what `language-service/diagnostics.ts` surfaces today. _Done: `usfm-parser-go/diagnostics/` plus diagnostic codes on `ParseError`; verified against the TS `getDiagnostics` on synthetic inputs (columns differ only where the TS lexer's attribute-rewind bug misreports positions — Go is correct there)._
- [x] **Unit tests** — table-driven tests for lexer, parser, and diagnostics; port the existing TS parser test cases; run the Berean Standard Bible corpus tests (`packages/usfm-parser-integration-tests/`) against the Go parser. _Done: unit tests landed with each ported package; corpus tests ported to `usfm-parser-go/integration/` (all 66 books, zero errors, full Bible parses in ~0.25 s). Includes `BenchmarkParsePsalms` (~24 ms, ~80k allocs on the dev machine — similar to TS's ~16 ms; allocation-reduction headroom exists if the perf revisit needs it)._

### Engine (LSP-like, simplified)

- [x] **Document store with sync** — engine keeps its own copy of each open document; API modeled on LSP lifecycle: `Open`, `ApplyChanges` (incremental edits with ranges + version numbers), `Close`. _Done: `usfm-parser-go/engine/store.go`; edits are `{from,to,text}` batches in UTF-16 offsets addressing the pre-batch document (CodeMirror `iterChanges` convention), version-gated, atomic on error, mutex-guarded (tested with `-race`)._
- [x] **Async processing** — parsing/analysis runs off the caller's goroutine; results are versioned so stale results can be discarded; debounce/coalesce rapid edits. _Done: `usfm-parser-go/engine/engine.go`; per-document worker goroutine, capacity-1 dirty signal coalesces edits (always analyzes the latest snapshot), optional debounce window, version-stamped `Analysis` (AST + diagnostics) delivered via `OnAnalysis` with monotonic versions — stale results are dropped, close discards in-flight work._
- [x] **Feature requests** — tailored to USFM editing needs (no go-to-definition): diagnostics (push or pull), semantic tokens / syntax classification for a range or viewport (replaces `language-service/classifier.ts`), completions for markers and book codes (replaces `language-service/completions.ts`), and whatever else `language-service/service.ts` currently provides (e.g. chapter/verse structure for the outline/preview). _Done: `engine/features.go` — `Diagnostics` (pull; push already via `OnAnalysis`), `Classify`/`ClassifyRange`, `Completions` (markers ported + book codes added), `Structure` (book/chapter outline replacing usfm-model's chapter listing). Classifier verified byte-identical to TS across the BSB corpus (`internal/classdump`)._
- [x] **Engine unit tests** — cover document sync (out-of-order versions, incremental edits), cancellation/staleness, and each feature request. _Done: 40+ tests across `store_test.go`, `engine_test.go`, `features_test.go`, and `coverage_test.go` — version gating, batch edits, UTF-16 edge cases, coalescing, monotonic delivery, close/reopen staleness (in-flight results from a previous open are discarded), pull-lag semantics, range clamping (verified against TS), and a whole-engine concurrency stress test; all run under `-race`._

### CLI tool

- [x] **`usfm` CLI** — standalone binary using the library: at minimum `check` (parse a file/dir, print diagnostics, non-zero exit on errors); consider `parse --json` for AST dumps to help debugging and test fixtures. _Done: `cmd/usfm` with `check [path ...]` (files, recursive dirs, `-` for stdin; compiler-style `file:line:col: severity: message [code]` output, 1-based positions; exit 1 on error diagnostics, 2 on usage/I/O problems) and `parse [-compact] <file|->` (JSON ParseResult dump, always exit 0). Unit tests in `cmd/usfm/main_test.go`; already covered by `go vet`/`go test ./...` in build.sh; BSB corpus checks clean in ~0.2 s._

### Wails integration (bible-edit)

- [x] **Bind the engine into the app** — add the engine to `apps/bible-edit` (Go dependency on the parser module, e.g. via `go.work` or a replace directive), expose bound methods on the app struct (regular function calls, not JSON-RPC), and push async results (diagnostics) to the frontend via Wails events. _Done: dependency via `replace` directive in `apps/bible-edit/go.mod`; new bound struct `UsfmService` (`usfm.go`) with `OpenDocument`/`ApplyChanges`/`CloseDocument` lifecycle plus `GetDiagnostics`, `GetStructure`, `ClassifyDocument`, `ClassifyRange`, `GetCompletions`; engine runs with zero debounce (engine-side coalescing) and pushes `{id, version, diagnostics}` as the `usfm:analysis` Wails event (AST never crosses the bridge). Unit tests in `usfm_test.go` (push on open/edit, stale-version rejection, close semantics, each pull) pass under `-race`; wailsjs bindings regenerated (`frontend/wailsjs/go/main/UsfmService.*`, models for `engine`/`usfm` namespaces). Note: the event payload type isn't in generated models (Wails only generates types reachable from bound signatures); the frontend adapter should type it as `{id: string} & DiagnosticsResult`._
- [x] **Frontend adapter** — implement the `usfm-controls` `language-service/protocol.ts` interface backed by the Wails bindings, keeping editor edits in sync with the engine (forward CodeMirror change sets as incremental updates). _Done: `protocol.ts` gained the stateful `UsfmLanguageClient` interface (open/applyChanges/close lifecycle, versioned pulls, `onAnalysis` push) plus result types; `language-service/document-sync.ts` provides `changesFromChangeSet` (CodeMirror `iterChanges` → `DocumentChange[]`) and `DocumentSync` (fire-and-forget ordered queue, enqueue-time version assignment, self-healing reopen on rejected updates — stale queued batches are rejected by the engine's version gate rather than double-applied; tested in `tests/document-sync.test.ts` incl. the convergence case). The Wails-backed implementation is `apps/bible-edit/frontend/src/language-client.ts` (`createWailsLanguageClient`), mapping bindings 1:1 and fanning out `usfm:analysis` events; frontend `usfm-controls.d.ts` shim updated (also fixed pre-existing missing `UsfmShellHandle`), `npx tsc --noEmit` clean, vite build OK. Wiring the editor/features onto this client is the next item._
- [x] **Switch features over** — errors panel + squiggles, syntax highlighting, and intellisense/completions in the editor all served by the Go engine; remove the TS parser from the `usfm-controls`/`usfm-model` runtime paths. _Done: all three features now flow through the injected `UsfmLanguageClient`. Each mounted `UsfmEditor` owns one engine document (unique id, opened/closed on mount/unmount) and forwards CodeMirror change sets via `DocumentSync`; viewport classification and completions are content-free requests ordered after pending edits (`DocumentSync.request`); diagnostics are engine-pushed — the editor applies squiggles itself and lifts them per tab to the shell errors panel (`onTabDiagnostics`), replacing the shell's 300 ms debounced validation entirely. Engine positions carry UTF-16 offsets used directly as CodeMirror positions (`posToOffset`, `onSelectDiagnostic`). `UsfmShell`/`UsfmPane`/`UsfmEditor` take a `languageClient` prop; bible-edit injects the Wails client (App.tsx), so in the app every parse runs in Go. Without injection, a new `createLocalLanguageClient()` (TS-service-backed, same versioned semantics, 150 ms analysis debounce) keeps stories/tests/library use working — it and the `usfm-model` preview/chapter consumers are the TS parser's remaining runtime imports, removed by the preview item. Tests: `local-client.test.ts` added; full controls suite (28 files), frontend tsc/vite, root build.sh all pass._
- [x] **Preview HTML rendering via the Go engine** — move `usfm-model`'s preview rendering (`render-preview-html.ts`) and the other `usfm-model` parser consumers (chapter lists, book picker) onto the new parser, e.g. an engine request that returns preview HTML (or the structure needed to render it) for the current document. _Done: `usfm-parser-go/preview/` ports the publication view model + HTML renderer (verified **byte-identical** to the TS renderer across all 66 BSB books × both verse-per-line modes; differential tool kept at `internal/previewdump`). Exposed as `preview.Render(text, Options)`, `Engine.RenderPreview(id, opts)`, and the Wails binding `RenderPreviewText`; protocol gained `renderPreview(text, options)` — deliberately text-based (not document-id) because the preview renders debounced snapshots and must work in preview-only view mode where no editor/engine document is mounted (revisit under the perf items). `UsfmPreview` now fetches HTML from the language client asynchronously (stale-render guard; local TS fallback otherwise), so in bible-edit preview parsing+rendering runs in Go off the UI thread. Chapter lists in the app path were already parser-free regex scans (perf work); `UsfmBookPicker`/`buildUsfmBookPickerGroups` and the BookNode helpers turned out to be story/library-only — not in the app runtime path. Tests: Go preview unit tests (ports of both TS test files) + engine/binding tests; all repo suites pass._

### Docs & cleanup

- [x] **README.md for the Go module** — architecture overview (library / engine / CLI), the LSP-like protocol (document sync, versioning, feature requests), how to use it as a library, CLI usage, and how bible-edit integrates it. _Done: `usfm-parser-go/README.md` covers the package layout, the UTF-16/byte position model, library examples (parse/diagnostics/preview), the engine's LSP-like protocol (lifecycle, version gating, edit-batch convention, coalescing, push vs pull, per-feature request table), CLI usage and exit codes, the bible-edit integration path (bindings → `usfm:analysis` event → frontend adapter/DocumentSync, incl. the TS fallback), the three-layer testing story (unit / BSB corpus / byte-identical differential dumps), and the inherited-quirk fidelity notes. `doc.go` package list updated to include `preview`; README commands verified to run._
- [x] **Update repo docs** — `AGENTS.md` / root `README.md`: new package layout, Go toolchain requirement, build commands. _Done: root README rewritten around the three top-level areas (packages/ workspace, usfm-parser-go module, apps/bible-edit) with updated diagram/table (usfm-parser + its integration tests marked reference-only), Go 1.22+/Wails/npm requirements, and accurate build.sh / per-area commands. AGENTS.md updated to match: layout incl. bibles/ corpus and todo.md, command table (Go vet/test/race, CLI, app build, `wails generate module`), corrected dependency/coverage notes (root check/test = model + controls only), plus new notes on the language-client protocol/fallback, the byte-identical differential-testing invariant, and the bible-edit npm/vite frontend + hand-maintained `usfm-controls.d.ts` shim. bible-edit README's stale Layout section updated and a "USFM engine" section added (UsfmService binding, `usfm:analysis` push, bindings regeneration)._

### Performance revisit

- [x] **Re-evaluate the existing debounce/throttle layers** — the perf work above (see "USFM editor typing performance") added debouncing because parsing was synchronous and on the UI thread: 300 ms validation debounce, 500 ms workspace model sync, 1.5 s preview HTML regeneration, scroll-sync pause while typing, viewport-only highlighting. With parsing async in Go, decide per feature whether the debounce still earns its latency or should shrink/go away. _Done — the deciding question per layer was whether the debounce guards parsing (now off-thread) or UI-thread work that asynchrony does not remove (React re-renders, DOM measurement/swaps, bridge payload size). **Removed:** the 100 ms highlight debounce — `usfmHighlighter` now keeps at most one classify request in flight, latest-wins: latency is one engine round trip, request volume self-clocks to round-trip time during bursts, and responses that arrive after a mid-flight edit/scroll are discarded and refetched (no stale application). **Shrunk + decoupled:** split-mode preview regeneration 1.5 s → 700 ms, now driven from the live editor buffer on document-change notifications instead of stacking on the 500 ms value lift (idle-to-preview ~2 s → ~0.7 s); the debounce itself stays because each refresh still ships the full document to the client and swaps the full-book preview DOM. External `value` replacements still reach the preview (they flow through the editor as document changes); value-lift echoes no longer trigger redundant renders. **Kept:** 500 ms value lift (guards React re-renders), 300 ms nav-marker scan (UI-thread scan; the "Move the chapter-marker scan to the engine" follow-up retires it), 500 ms scroll-sync typing idle + 120 ms viewport/scroll reads (DOM measurement), and the local TS client's 150 ms analysis debounce (that fallback still parses on the UI thread). The shell's 300 ms validation debounce was already gone (engine push). Also fixed while there: `UsfmEditor` no longer materializes `doc.toString()` on every keystroke (deferred to debounce emission), and a pending debounced `onChange` is flushed instead of dropped on unmount — dropping it lost the last ≤500 ms of edits when a view-mode switch remounted the editor._
- [x] **Prefer engine-side coalescing over frontend debouncing** — forward edits to the engine immediately (cheap sync of the document copy) and let the engine coalesce/cancel stale parses by version; the frontend then just applies whatever versioned results arrive, instead of gating requests with timers. _Done: edits were already forwarded immediately (DocumentSync), diagnostics push-applied, and highlighting latest-wins (previous item); this item converted the last timer-gated request — the split-pane preview, which shipped the full document text over the bridge per refresh. The protocol gained `renderPreviewDocument(id, options) → PreviewResult {version, html}`, served by the existing `Engine.RenderPreview` through the new `RenderPreviewDocument` Wails binding (bindings regenerated) and by the local TS client. `UsfmEditor` reports its engine document id via a new `onDocumentIdChange` prop; `UsfmPane` passes it to the split-mode `UsfmPreview`, which prefers the document-id render and falls back to text if the document closes mid-request (and stays text-based in preview-only mode, where no engine document exists — that ownership question remains with the chapter-scan follow-up). The 700 ms preview timer stays deliberately: it gates the full-book DOM swap, not the request. The request is now a handle, and the engine renders its own synced copy — which includes all edits forwarded up to render time, so the rendered snapshot is never staler than the debounced trigger. Tests: Go binding test (`TestRenderPreviewDocument`), local-client test, full controls suite, frontend tsc + vite build all pass._
- [x] **Watch the new costs asynchrony introduces** — Wails bridge call overhead per keystroke, serialization of large results (semantic tokens, preview HTML), and stale-result flicker (e.g. squiggles or highlights lagging the text); batch or range-scope results where needed. _Done — audited each named cost; one fix fell out. **Bridge calls per keystroke:** 1× `ApplyChanges` (payload = the edit batch, bytes), at most one in-flight `ClassifyRange` (viewport-scoped, latest-wins — during fast typing roughly one per round trip, not per keystroke), and ≤1 coalesced `usfm:analysis` push; while typing nothing else crosses the bridge (preview only at 700 ms idle and by id, completions only inside `\\...` prefixes). ~2–3 small IPC messages per keystroke — no batching needed. **Large results:** semantic tokens are already range-scoped (viewport + margin, tens of KB worst case; could halve by dropping the unused line/column/byte fields — frontend reads only `offset` — if end-to-end profiling says it matters); preview HTML is full-book by nature but debounced and id-requested, with the DOM swap as the real cost (profiled in the next item); diagnostics pushes are whole-doc and proportional to error count — fine for real books, worth remembering that a pathological non-USFM file pushes its thousands of errors per analysis. **Stale-result flicker:** highlighting (skip-if-dirty refetch) and preview (generation guard + engine-copy render) were already handled; the remaining case was squiggles/errors: `UsfmEditor` applied every `usfm:analysis` push, so an analysis of version N landing after the editor had forwarded N+k painted pre-edit offsets, fighting CodeMirror's own (correct) mapping of the existing squiggles through the edits. Now gated: pushes older than `DocumentSync.version` are skipped — safe because the client is guaranteed to re-analyze after every edit, so a push at the forwarded version always follows. Covered by the new `usfm-editor-analysis.test.tsx` (stale skipped, fresh applied, other-document pushes ignored); full suites + frontend tsc pass. Real-world numbers (typing latency, DOM-swap cost, token payload) belong to the end-to-end verification below._
- [ ] **Verify performance end-to-end** — profile typing in large books (e.g. Psalms) with the Go engine doing validation/highlighting/preview; confirm it's at least as smooth as the tuned TS setup before deleting the old optimizations. _First manual run (PSA.usfm, split mode, `wails dev` on the Chromebook) failed badly: typing grew increasingly janky and the caret jumped to the start of the book, inserting keystrokes there. Root causes found and fixed: **(1) stale-echo race in `UsfmEditor`** — the external-`value` effect runs after paint, so under main-thread congestion the next debounced emission could land before the previous echo's effect ran; the stale echo was then misread as an external change and dispatched as a full-document replace (reverting the newest edits, caret → 0 — the data corruption the user saw). Now guarded by an `echoPending` flag: while an emission is round-tripping, differing `value` props are skipped until the fresh echo clears the flag (test: "ignores stale value echoes..."). **(2) Preview swap congestion** — at 700 ms the full-book innerHTML swap + layout (seconds on software-rendered WebKitGTK) fired at every between-sentences pause; debounce restored to 1500 ms, and `.usfm-chapter` now gets `content-visibility: auto` so a swap lays out only on-screen chapters. **(3) Scroll-sync ping-pong** — preview→editor sync ran un-debounced on every preview scroll event (forced layout + 290 KB scans) and the swap's scroll perturbation could drag the editor to the book start via `scrollTop = 0` fallbacks; now debounced (120 ms), paused while typing (500 ms, mirroring the other direction), suppressed for 300 ms after a swap, and the preview's scroll position is restored after each swap (`UsfmPreview.onRendered`). Note `wails dev` also runs React StrictMode/dev builds — retest should compare against the production build. Second manual finding: with the same book open in two tab groups, edits stopped syncing to the other tab — a regression from the echo guard: deactivating a tab flushes its editor, and a *clean* flush emitted a no-op change that armed the guard with an echo that could never arrive (`workspaceSetTabValue` ignores identical values → no re-render), so the stuck guard then dropped all cross-tab `value` updates. Fixed by never emitting content equal to the last emission (`emitChangedDocument` guards all emission paths — timer, immediate, flush, unmount), restoring the guard's invariant that armed ⇒ echo inbound; regression test "a clean flush emits nothing…" added. Third manual finding: with the same book in two tab groups, the *second* tab's syntax highlighting landed on seemingly random letters. Every headless layer proved exact under differential tests (Go range-classify rebase on real PSA incl. non-ASCII — kept as `engine/classify_psa_diff_test.go`; the full-replace store pipeline; JSON position contract; local-client twin-editor sync — kept as `usfm-editor-twin-sync.test.tsx`), which narrowed it to the untestable layer: the receiving tab was the only place shipping the whole ~290 KB book over the Wails bridge (a full-document replace per 500 ms cross-tab echo), with `DocumentSync` swallowing any bridge failure silently — a failed replace + failed silent reopen leaves the engine copy frozen on old text, and classify offsets against frozen text look exactly like random-letter highlights. Fixed at the source: the external-value apply now dispatches a minimal prefix/suffix-trimmed edit instead of a full replace (bytes instead of the book per echo; decorations/caret/scroll survive in the receiving tab), and `DocumentSync` now logs rejected updates and failed reopens to the console instead of dying silently. Awaiting a re-run of the manual test plan._

### Follow-ups

- [ ] **Move the chapter-marker scan to the engine** — `UsfmPane` still scans the document text on the UI thread for chapter navigation and scroll sync (`listChapterMarkersInUsfm` / `bookIdMarkerOffsetInUsfm`, debounced 300 ms in split mode). It's "just" a regex line scan, but regex is still parsing, and the Go engine already computes the same information properly (`Structure` returns books and chapters with positions from the real parse — no separate grammar or scan needed). Serving it from the engine gets the scan off the UI thread (taking advantage of the async engine), reuses the analysis that already runs on every edit, and retires one more hand-rolled parallel parser. Design constraint, partially solved: the pane now receives the mounted editor's document id via `onDocumentIdChange` (added for the document-id preview render), so `Structure` can be served whenever an editor is mounted (edit/split modes); preview-only view mode still has no engine document — either lift document ownership to the pane/tab, or keep a text-based fallback there like the preview does.

- [ ] **Drop the TS parser from the app bundle** — the app no longer *runs* TS-parser code (Go engine serves diagnostics/highlighting/completions/preview), but the bundle still *contains* it via static fallback imports: `language-service/local-client.ts` (+ `diagnostics/classifier/completions/service.ts`) and `usfm-controls`' re-exports of `usfm-model` (`renderPreviewHtml`, `buildUsfmBookPickerGroups`, BookNode helpers). To exclude it, either make the local fallback a dynamic import (or a separate entry point stories/tests opt into) and stop re-exporting the parser-backed `usfm-model` API from the package index, or accept the dead weight. Also decide the fate of the story-only `UsfmBookPicker` (its `buildUsfmBookPickerGroups` parses every file).

- [ ] **Reduce Go parser allocations if the perf revisit needs it** — `BenchmarkParsePsalms` shows ~24 ms / ~80k allocs for Psalms on the dev machine, comparable to (not faster than) the TS parser's ~16 ms. The port inherits the TS design's allocation patterns: a token struct per lexeme, a node per text run, string concatenation when merging text tokens, and a fresh map per attributed node. Off-UI-thread parsing makes this tolerable, but if lower latency is wanted (e.g. for per-keystroke incremental reparses), profile and consider: preallocating the token slice, building merged text via `strings.Builder` or source slicing, pooling nodes, and lazy attribute maps. Tie into the "Verify performance end-to-end" item. — the Go parser port reproduces these faithfully (verified identical on the BSB corpus; also noted in the `parser` package comment): text after a chapter number is silently dropped (`\c 1 extra` loses "extra"); `\fig` in inline context (e.g. inside a paragraph) is reported as "Unknown marker" instead of being parsed as a figure; note-caller splitting collapses interior whitespace in the note's first text run and keeps a trailing space before the end marker (see `TestNoteTextKeepsTrailingSpace`). Decide per quirk whether it's correct USFM handling or a bug; fix in both parsers or document as intentional.
- [ ] **Investigate suspected typos carried over from the TS grammar** — the grammar port copied two oddities faithfully rather than fixing them silently: the cell list has `tch12` where the pattern suggests `thc12` should be, and `wl` has a default attribute (`lang`) but appears in no category (so it's `unknown`). Check against the USFM 3.x spec and fix both `grammar.ts` and `usfm-parser-go/grammar/grammar.go` (or document why they're intentional).

## Key files

- `packages/usfm-parser/src/{grammar,lexer,parser,types}.ts` — old parser to port
- `packages/usfm-controls/src/language-service/` — existing LSP-like TS layer (protocol, service, diagnostics, classifier, completions) to be backed by the Go engine
- `packages/usfm-model/src/` — other consumers of the old parser (preview rendering, chapter lists, book picker)
- `packages/usfm-parser-integration-tests/` — Berean corpus tests to port
- `apps/bible-edit/{main.go,app.go}` — Wails app where the engine gets bound

